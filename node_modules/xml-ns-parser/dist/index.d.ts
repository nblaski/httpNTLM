export declare function parseXml(xmlText: string, entityLookup?: (path: string) => Promise<string>): Promise<Xml>;
declare type writeOptions = {
    pretty: boolean;
    indent: number;
    quote: string;
};
export declare function writeXml(xml: Xml, options?: Partial<writeOptions>): string;
export declare function getTagname(name: string, scope: Scope, includeDefaultTypes?: boolean): TagName;
declare type Scope = {
    [prefix: string]: string;
};
export declare type Xml = {
    children: Xml[];
    attributes: {
        [name: string]: string;
    };
    name: TagName;
    scope: Scope;
    text: string;
};
export declare type TagName = {
    local: string;
    namespace: string;
};
export {};
